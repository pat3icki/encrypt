# encrypt
A Go cryptography toolkit providing a unified interface for encryption (AES-GCM, XChaCha20, Branca) and hashing (Argon2id, Bcrypt). It uses a 3-byte prefix system to automatically detect modes during decryption and hash comparison, simplifying secure data storage.
